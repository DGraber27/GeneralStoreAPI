using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;

namespace GeneralStoreAPI.Controllers
{
    public class TransactionController : ApiController
    {
        //Create Transaction (POST)
            //Verify product is in stock
            //check there is enough product for transaction
            //remove products that were bought

        //Get all transactions (Transaction History) (GET)

        //Get all transactions by Customer ID(GET)

        //Get a Transaction by its ID(GET)

        //Update existing Transaction by its ID (PUT)
            // Verify product changes
            //Update Product Inventory

        //Delete existing transaction by its Id (DELETE)
               //Update Product Inventory to reflec tupdated Transaction

    }
}
